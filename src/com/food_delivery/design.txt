Entity
Restautant
id
name
orders (Order table id)
menu
--------------
methods
addMenu() -> params (id) -> return void -> enter menu type wise entry in menu

Menu
id
name
type (Panjabi, chinses)
items (Item table id)
--------------
methods
addItem() -> params(id, type) -> return void -> add entry to items arraylist

Item
id
name
price
type (VEG, NON_VEG, BEVARAGE)
--------------
methods
add() -> params (name, price) -> return void -> add entry to items arraylist

Order
id
status (PLACED,CONFIRMED, PREPRATING, OUT_OF_DELIVERY, DELIVERED)
items (continsing id(Item table's id) and quantity )
patments (id)
total_price
total_tax (cgst, igst)
--------------
methods
create() -> params (items) -> return void -> create order with given menu item + quantity
calculateOrder() -> param(id) -> return amount 
assignDelivertPartner -> partner id -> return void

Customer (Extends person class)
id
name
--------------
methods
create() -> name

Person
id
name

DeliveryAgent (Extends person class)
id
orders
--------------
methods
trackOrder() -> should return only status string or given order id

Payment interface
----------
methods
dispayPayment()

UPI (Extends payment)
id
upi
amount
----------
methods
add() -> param(amount, upi) -> return void

Cards (Extends payment)
id
number
cvv
expiry_date
amount
add() -> param(amount, number, cvv, expiry_date) -> return void


compile => javac -d out $(find src -name "*.java")
run => java -cp out Main



















































































































































































































